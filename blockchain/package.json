{
  "name": "voting-blockchain",
  "version": "1.0.0",
  "description": "Blockchain component for voting API",
  "main": "truffle-config.js",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --network development",
    "deploy": "npm run compile && npm run migrate"
  },
  "dependencies": {
    "truffle": "^5.11.5",
    "web3": "^1.10.4"
  },
  "devDependencies": {},
  "author": "",
  "license": "MIT"
}
